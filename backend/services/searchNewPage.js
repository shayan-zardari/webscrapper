// import puppeteer from "puppeteer";

// async function puppeteerSearch(searchString) {
//     const browser = await puppeteer.launch();
//     const page = await browser.newPage();


//     const result = await page.goto("https://www.google.com");
    
//     // await page.waitForSelector('input[name="q"]', { visible: true });

//     // const result = await page.type("input[name='q']",searchString);

//     await browser.close();

//     return result;
// };


// puppeteerSearch("instagram");

// export default puppeteerSearch;

